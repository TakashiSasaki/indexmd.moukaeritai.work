# Visual Analysis Contract v0.2.0-draft.1

- **Contract Name**: Visual Analysis Schema
- **Version**: v0.2.0-draft.1
- **Status**: **Draft** (Actively calibrating under evaluation)
- **Purpose**: Defines the inner schema produced by the computer vision analysis engine. It contains image-observable/inferable visual information only (e.g. image kinds, environment context, detected categories, detailed visible element labels, and recognized OCR text blocks) without operational metadata.
- **Primary Producer**: Image analysis vision pipeline powered by multimodal Gemini models.
- **Primary Consumers**: `ImageAnalysisRecord` visual analysis field, public sample regression evaluation harness, and visual catalog tools.
- **Related App Docs**: `docs/visual-analysis-schema.md`

---

## Files

- **Schema File**: [schema.json](schema.json)
- **Example Files**:
  - [examples/minimal.json](examples/minimal.json): A simple visual analysis payload with minimal categories and scene context.
  - [examples/full.json](examples/full.json): A complete visual analysis output covering landscape/product details and full optical character recognition (OCR) findings.

---

## Compatibility and Lifecycle Policies

- **Breaking Change Policy**: This contract is currently in a **Draft** status. While we aim to maintain stability, breaking changes are permitted during active validation. All breaking changes will be documented clearly in the contract's `CHANGELOG.md`.
- **Compatibility Notes**: Downstream parsers must expect optional properties to be added or modified safely. We recommend lenient parsing policies (e.g., ignoring unknown fields) to maintain compatibility.

---

## ⚠️ Known Limitations

1. **OCR Block Segmentation**: OCR elements (found in `visibleText`) are represented as a list of independent coordinates and texts. In this draft version, there is no structural nesting to model reading orders (e.g., multi-column text flow groups). Consumers must reconstruct columns based on coordinate bounds if spatial grouping is required.
2. **Confidence Score Scaling**: Confidence values across different classes and labels are mapped to a range of `[0.0, 1.0]`. Note that these are subjective heuristic probabilities generated by the multimodal model and should not be treated as absolute mathematical probabilities.
